# 🎨 PROMPTS OPTIMIZADOS PARA IMAGEN 3 (GEMINI)

## ⚠️ IMPORTANTE PARA IMAGEN 3

Imagen 3 de Gemini NO genera sprite sheets automáticamente en formato horizontal.
**SOLUCIÓN:** Generamos cada frame individual y luego los unimos manualmente.

---

## 🎯 ESTRATEGIA RECOMENDADA

### OPCIÓN A: Frame Individual (RECOMENDADA)
Genera cada frame por separado y luego los unes en Photoshop/GIMP/online.

### OPCIÓN B: Pedir explícitamente formato horizontal
Usar prompts muy específicos con énfasis en el layout.

---

# 📋 PROMPTS PARA IMAGEN 3 - OPCIÓN A (Frame Individual)

## 🎮 FORMATO DEL PROMPT

Para cada frame individual:

```
A single [action] frame of a [character description], pixel art style, top-down view, 64x64 pixels, game sprite, transparent background, retro 16-bit style, clean pixel art, no text, no UI, isolated character
```

---

# 👤 CABALLERO (KNIGHT) - FRAMES INDIVIDUALES

## IDLE Frame 1
```
A single idle standing frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, breathing pose, centered character
```

## IDLE Frame 2
```
A single idle standing frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, slight breathing motion, centered character
```

## IDLE Frame 3
```
A single idle standing frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, breathing pose variation, centered character
```

## IDLE Frame 4
```
A single idle standing frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, return to neutral pose, centered character
```

---

## RUN Frame 1
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, left leg forward, centered character
```

## RUN Frame 2
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, mid-stride motion, centered character
```

## RUN Frame 3
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, right leg forward, centered character
```

## RUN Frame 4
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, mid-stride opposite, centered character
```

## RUN Frame 5
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, left leg forward again, centered character
```

## RUN Frame 6
```
A single running frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, holding sword and shield, game sprite, transparent background, retro 16-bit style, clean pixel art, completing run cycle, centered character
```

---

## ATTACK Frame 1
```
A single attack frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, sword raised preparing to strike, game sprite, transparent background, retro 16-bit style, clean pixel art, wind-up pose, centered character
```

## ATTACK Frame 2
```
A single attack frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, sword mid-swing, game sprite, transparent background, retro 16-bit style, clean pixel art, striking motion, centered character
```

## ATTACK Frame 3
```
A single attack frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, sword at end of swing, game sprite, transparent background, retro 16-bit style, clean pixel art, follow-through, centered character
```

## ATTACK Frame 4
```
A single attack frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, returning to ready stance, game sprite, transparent background, retro 16-bit style, clean pixel art, recovery pose, centered character
```

---

## HIT Frame 1
```
A single hit reaction frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, recoiling from impact, game sprite, transparent background, retro 16-bit style, clean pixel art, hurt expression, centered character
```

## HIT Frame 2
```
A single hit reaction frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, recovering from hit, game sprite, transparent background, retro 16-bit style, clean pixel art, returning to stance, centered character
```

---

## DEATH Frame 1
```
A single death frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, starting to fall, game sprite, transparent background, retro 16-bit style, clean pixel art, losing balance, centered character
```

## DEATH Frame 2
```
A single death frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, falling down, game sprite, transparent background, retro 16-bit style, clean pixel art, mid-fall, centered character
```

## DEATH Frame 3
```
A single death frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, almost on ground, game sprite, transparent background, retro 16-bit style, clean pixel art, nearly defeated, centered character
```

## DEATH Frame 4
```
A single death frame of a medieval knight character, pixel art style, top-down view, 64x64 pixels, blue armor, white cape, lying defeated on ground, game sprite, transparent background, retro 16-bit style, clean pixel art, final death pose, centered character
```

---

# 📋 PROMPTS PARA IMAGEN 3 - OPCIÓN B (Sprite Sheet Horizontal)

## ⚡ PROMPT MEJORADO PARA SPRITE SHEET

Si quieres intentar que genere todo en una imagen:

```
Pixel art sprite sheet showing 4 animation frames in a single horizontal row, medieval knight character, top-down view, each frame 64x64 pixels, total image 256x64 pixels, blue armor with white cape, idle breathing animation, frames arranged left to right [frame1][frame2][frame3][frame4], retro 16-bit game style, transparent or white background, clean pixel art, no gaps between frames, game asset format
```

### PALABRAS CLAVE IMPORTANTES PARA IMAGEN 3:
- "horizontal row"
- "frames arranged left to right"
- "single image"
- "sprite sheet"
- "game asset format"
- "[frame1][frame2][frame3][frame4]"

---

# 🎨 PROMPTS SIMPLIFICADOS PARA CADA PERSONAJE

## CABALLERO - IDLE (Sprite Sheet)
```
Pixel art sprite sheet, 4 frames horizontal row, medieval knight, top-down view, idle breathing animation, blue armor white cape, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## CABALLERO - RUN (Sprite Sheet)
```
Pixel art sprite sheet, 6 frames horizontal row, medieval knight, top-down view, running animation, blue armor white cape, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## CABALLERO - ATTACK (Sprite Sheet)
```
Pixel art sprite sheet, 4 frames horizontal row, medieval knight, top-down view, sword attack animation, blue armor white cape, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## CABALLERO - HIT (Sprite Sheet)
```
Pixel art sprite sheet, 2 frames horizontal row, medieval knight, top-down view, hit reaction animation, blue armor white cape, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## CABALLERO - DEATH (Sprite Sheet)
```
Pixel art sprite sheet, 4 frames horizontal row, medieval knight, top-down view, death falling animation, blue armor white cape, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# 🗡️ GUERRERO - Sprite Sheets

## IDLE
```
Pixel art sprite sheet, 4 frames horizontal row, barbarian warrior, top-down view, idle animation, red armor fur cape holding axe, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## RUN
```
Pixel art sprite sheet, 6 frames horizontal row, barbarian warrior, top-down view, running animation, red armor fur cape holding axe, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## ATTACK
```
Pixel art sprite sheet, 4 frames horizontal row, barbarian warrior, top-down view, axe swing animation, red armor fur cape, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## HIT
```
Pixel art sprite sheet, 2 frames horizontal row, barbarian warrior, top-down view, hit reaction animation, red armor fur cape, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## DEATH
```
Pixel art sprite sheet, 4 frames horizontal row, barbarian warrior, top-down view, death animation, red armor fur cape, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# 🔮 MAGO - Sprite Sheets

## IDLE
```
Pixel art sprite sheet, 4 frames horizontal row, wizard mage, top-down view, idle animation, purple robes pointed hat holding staff, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## RUN
```
Pixel art sprite sheet, 6 frames horizontal row, wizard mage, top-down view, running animation, purple robes pointed hat holding staff, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## ATTACK
```
Pixel art sprite sheet, 4 frames horizontal row, wizard mage, top-down view, spell casting animation, purple robes pointed hat staff raised, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## HIT
```
Pixel art sprite sheet, 2 frames horizontal row, wizard mage, top-down view, hit reaction animation, purple robes pointed hat, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## DEATH
```
Pixel art sprite sheet, 4 frames horizontal row, wizard mage, top-down view, death animation, purple robes pointed hat falling, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# 🏹 ARQUERO - Sprite Sheets

## IDLE
```
Pixel art sprite sheet, 4 frames horizontal row, archer ranger, top-down view, idle animation, green hood cloak holding bow, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## RUN
```
Pixel art sprite sheet, 6 frames horizontal row, archer ranger, top-down view, running animation, green hood cloak holding bow, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## ATTACK
```
Pixel art sprite sheet, 4 frames horizontal row, archer ranger, top-down view, shooting arrow animation, green hood cloak bow drawn, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## HIT
```
Pixel art sprite sheet, 2 frames horizontal row, archer ranger, top-down view, hit reaction animation, green hood cloak, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## DEATH
```
Pixel art sprite sheet, 4 frames horizontal row, archer ranger, top-down view, death animation, green hood cloak falling, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# 🗡️ PÍCARO - Sprite Sheets

## IDLE
```
Pixel art sprite sheet, 4 frames horizontal row, rogue assassin, top-down view, idle animation, black leather armor hood dual daggers, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## RUN
```
Pixel art sprite sheet, 6 frames horizontal row, rogue assassin, top-down view, running animation, black leather armor hood dual daggers, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## ATTACK
```
Pixel art sprite sheet, 4 frames horizontal row, rogue assassin, top-down view, dagger strike animation, black leather armor hood, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## HIT
```
Pixel art sprite sheet, 2 frames horizontal row, rogue assassin, top-down view, hit reaction animation, black leather armor hood, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## DEATH
```
Pixel art sprite sheet, 4 frames horizontal row, rogue assassin, top-down view, death animation, black leather armor hood falling, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# ⚔️ PALADÍN - Sprite Sheets

## IDLE
```
Pixel art sprite sheet, 4 frames horizontal row, holy paladin, top-down view, idle animation, golden armor white cape holy sword shield, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## RUN
```
Pixel art sprite sheet, 6 frames horizontal row, holy paladin, top-down view, running animation, golden armor white cape holy sword shield, 64x64 per frame, total 384x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## ATTACK
```
Pixel art sprite sheet, 4 frames horizontal row, holy paladin, top-down view, holy strike animation, golden armor white cape glowing sword, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## HIT
```
Pixel art sprite sheet, 2 frames horizontal row, holy paladin, top-down view, hit reaction animation, golden armor white cape, 64x64 per frame, total 128x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

## DEATH
```
Pixel art sprite sheet, 4 frames horizontal row, holy paladin, top-down view, death animation, golden armor white cape falling heroically, 64x64 per frame, total 256x64 pixels, frames side by side, retro game style, clean pixels, transparent background
```

---

# 💡 TIPS ESPECÍFICOS PARA IMAGEN 3

1. **Sé muy específico** con "horizontal row" y "frames side by side"
2. **Menciona el tamaño total** de la imagen (ej: 256x64 pixels)
3. **Usa "sprite sheet"** explícitamente
4. **Si no funciona:** Genera frames individuales y únelos con una herramienta online
5. **Mantén prompts cortos** - Imagen 3 funciona mejor con prompts concisos
6. **Regenera** si no sale bien - a veces toma 2-3 intentos

---

# 🛠️ HERRAMIENTAS PARA UNIR FRAMES

Si generas frames individuales, úsalos aquí:

- **Piskel** (online): https://www.piskelapp.com/
- **Photopea** (online): https://www.photopea.com/
- **Sprite Sheet Packer** (online): https://www.codeandweb.com/free-sprite-sheet-packer

---

# ✅ CHECKLIST

- [ ] Prompt incluye "horizontal row"
- [ ] Prompt incluye "sprite sheet"
- [ ] Prompt especifica número de frames
- [ ] Prompt especifica tamaño total
- [ ] Prompt incluye "frames side by side"
- [ ] Estilo es "pixel art" y "retro game style"
- [ ] Fondo es "transparent background"

---

¡Suerte con Imagen 3! 🎨✨
